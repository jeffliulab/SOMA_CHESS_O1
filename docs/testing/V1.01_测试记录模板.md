# V1.01 Week 1 + Week 2 测试记录模板

> 使用方式：统一联调当天直接复制这份模板，边测边填。

---

## 基本信息

- 测试日期：
- 测试人：
- 相机 sender 模式：
  - `tuning / locked / adaptive`
- 当前光照条件：
  - `台灯单开 / 混合室内光 / 其他`
- 机械臂串口：
- 手柄设备：
- 备注：

---

## Phase 0：teleop / 驱动

- `usbipd list` 已确认：`是 / 否`
- `/dev/ttyUSB*` 正常：`是 / 否`
- `/dev/input/event*` 正常：`是 / 否`
- `scripts/start_teleop_wsl_gamepad.sh` 正常启动：`是 / 否`
- `/joint_states` 正常：`是 / 否`
- 手柄控制正常：`是 / 否`
- 备注：

---

## Phase 1：W1.4A locked baseline

### 冷启动 1

- 亮度稳定：`是 / 否`
- 白平衡稳定：`是 / 否`
- 焦点稳定：`是 / 否`
- 备注：

### 冷启动 2

- 亮度稳定：`是 / 否`
- 白平衡稳定：`是 / 否`
- 焦点稳定：`是 / 否`
- 备注：

### 冷启动 3

- 亮度稳定：`是 / 否`
- 白平衡稳定：`是 / 否`
- 焦点稳定：`是 / 否`
- 备注：

### 连续观察 2 分钟

- 有无明显曝光抽动：
- 有无明显偏色漂移：
- 有无明显失焦：
- 结论：`通过 / 不通过`

---

## Phase 2：W1.6 相机内参

- ChArUco 图像数量：
- `frames_used`：
- `reprojection_error_px`：
- `camera_intrinsics.yaml` 已生成：`是 / 否`
- 结论：`通过 / 不通过`
- 备注：

---

## Phase 3：W1.6 workspace + eye-to-hand

- `eye_to_hand.yaml` 已生成：`是 / 否`
- `workspace.yaml` 已生成：`是 / 否`
- `pixel_to_world_homography` 已写入：`是 / 否`
- `board_roi_norm` 合理：`是 / 否`
- `reachable_test_points_xy_m` 已生成：`是 / 否`
- 结论：`通过 / 不通过`
- 备注：

---

## Phase 4：W1.8 reachability

### 测点结果

| 点位 | 5 次是否稳定到达 | 是否碰撞 | 是否越界 | 备注 |
|---|---|---|---|---|
| 角点 1 |  |  |  |  |
| 角点 2 |  |  |  |  |
| 角点 3 |  |  |  |  |
| 角点 4 |  |  |  |  |
| 中心 |  |  |  |  |
| 容器区 |  |  |  |  |

- `reachability_results.yaml` 已填写：`是 / 否`
- 已回写 `workspace.yaml`：`是 / 否`
- 结论：`通过 / 不通过`

---

## Phase 5：Week 2 `/find_object`

### 棋盘

- 查询词：
- 返回 `success`：
- 返回 `score`：
- `pixel_x / pixel_y` 是否合理：`是 / 否`
- `world_x / world_y` 是否合理：`是 / 否`
- 备注：

### 容器

- 查询词：
- 返回 `success`：
- 返回 `score`：
- `pixel_x / pixel_y` 是否合理：`是 / 否`
- `world_x / world_y` 是否合理：`是 / 否`
- 备注：

### 通用前景目标

- 查询词：
- 返回 `success`：
- 返回 `score`：
- `pixel_x / pixel_y` 是否合理：`是 / 否`
- `world_x / world_y` 是否合理：`是 / 否`
- 备注：

### 综合结论

- `/find_object` 是否达到 geometry-first 可用水平：`是 / 否`
- 是否需要继续增强：
  - `棋盘检测`
  - `容器定位`
  - `前景分割`
  - `pixel -> world`

---

## Phase 6：W1.4B adaptive

### 场景 1：台灯单开

- 是否振荡：
- 是否误调：
- 备注：

### 场景 2：混合室内光

- 是否振荡：
- 是否误调：
- 备注：

### 场景 3：短时遮挡

- 是否振荡：
- 是否误调：
- 备注：

### 综合结论

- adaptive 是否可用：`是 / 否`
- 关闭 adaptive 后是否回到 locked baseline：`是 / 否`
- 是否允许进入下一轮继续用：`是 / 否`

---

## 最终结论

- `W1.4A`：`通过 / 不通过`
- `W1.6`：`通过 / 不通过`
- `W1.8`：`通过 / 不通过`
- `Week 2 geometry-first`：`通过 / 不通过`
- `W1.4B`：`通过 / 不通过`

### 本轮遗留问题

1.
2.
3.

### 下一轮动作

1.
2.
3.
